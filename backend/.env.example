# ================================================================================
# Backend
# ================================================================================
POSTGRES_DB            = "dementia_chat_db"
POSTGRES_USER          = 
POSTGRES_PASSWORD      = 
DJANGO_SETTINGS_MODULE = "backend.settings"
TZ                     = "UTC"
GOOGLE_API_KEY         = 
GOOGLE_APPLICATION_CREDENTIALS = "google-stt-key.json" # Need to get an API key and put it in /backend
PEXELS_KEY             =

# For development (localhost)
DEV_APP_ROUTE = "http://localhost:8000" #  "" Nothing for deployment mode

# --------------------------------------------------------------------------------
# Llama API
# --------------------------------------------------------------------------------
# Local vs. cloud environment; local uses dummy_LLM, cloud uses GPU instance
APP_ENVIRONMENT  = "local"

# Access to the external GPU VM instance
LLM_BASE_URL      =
LLM_GATEWAY_TOKEN =

